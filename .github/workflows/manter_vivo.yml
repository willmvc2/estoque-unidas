name: Manter Streamlit Acordado

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  acordar:
    runs-on: ubuntu-latest
    steps:
      - name: Acessar o site do Streamlit
        run: |
          curl -I https://estoque-unidas.streamlit.app/